class TaxCalculator {
  calculateTax(employee) {
    if (employee.empType == "fullTime") {
      // Tax Calculation for Full time employee
    } else if (employee.empType == "partTime") {
      // Tax Calculation for part time employee
    }
  }
}
